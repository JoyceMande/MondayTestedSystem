@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Index";
}

<div class="form-horizontal">
    <div class="form-group">
       
        <section class="col-sm-6">
            <div class="col-md-10">@Html.ActionLink("Create Role", "Create", "Roles")</div>
        </section>
        <section class="col-sm-6">
            @Html.ActionLink("Assign Role To User", "RegisterRole", "Account")
        </section>
    </div>
</div>
@*<section class="col-sm-6">
        <table class="table-bordered ">
            <div style="padding: 10px; margin: 10px;" class="btn btn-info">

            </div>
        </table>
    </section>
    <section class="col-sm-6">
        <table class="table-bordered">
            <div style="padding: 10px; margin: 10px;" class="btn btn-info">

            </div>
        </table>
    </section>*@

<br />
<label class="control-label col-md-2">List of Roles</label>

<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-condensed table-striped table-responsive List-table">
            <tr>
                <th class="c">Role Name</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="c">@item.Name</td>
                </tr>
            }
        </table>
    </div>
</div>
